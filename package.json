{
  "name": "BelezaNaWeb.Front",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "projects/*",
    "shared/*"
  ],
  "scripts": {
    "start:checkout": "yarn workspace @belezanaweb/checkout dev",
    "test": "jest",
    "coverage": "jest --coverage",
    "build": "yarn workspace @belezanaweb/checkout build",
    "export": "yarn workspace @belezanaweb/checkout export"
  },
  "dependencies": {
    "jest": "^27.0.6",
    "jest-directory-named-resolver": "^0.3.0"
  },
  "devDependencies": {
    "dotenv": "^10.0.0"
  }
}
