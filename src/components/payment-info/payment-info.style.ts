import { Card } from "components";

import styled from 'styled-components'

export const PaymentInfoStyled = styled(Card)`
  p {
    font-size: 14px;
    color: #000;
    text-transform: uppercase;
    line-height: 17px;
    letter-spacing: 0;
  }
`
