import styled from 'styled-components'
import { Form as UnformForm } from '@unform/web'

export const StyledForm = styled(UnformForm)`
  display: flex;
  flex-direction: column;
  gap: 25px;
`
export const FormControlGroup = styled.div`
  display: flex;
  flex: 1;
  gap: 20px;
`
