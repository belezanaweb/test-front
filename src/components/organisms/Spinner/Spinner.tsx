import * as Styles from './Spinner.styles'

const Spinner: React.FC = () => {
    return (
        <Styles.Container>
            <Styles.Spinner />
        </Styles.Container>
    )
}

export default Spinner
