import React, { Component } from 'react';

class Header extends Component {
  render() {
    return (
      <header>
        SACOLA PAGAMENTO CONFIRMAÇÃO
      </header>
    );
  }
}

export default Header;