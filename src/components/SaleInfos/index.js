import styled from 'styled-components'
import px2vw from '../../utils/px2vw'

const SaleInfos = styled.div`
  padding: ${px2vw(20)} ${px2vw(20)} 0 ${px2vw(20)};

  border-radius: 3px;
  border: 1px solid #ccc;

  flex-direction: column;
`

export default SaleInfos
