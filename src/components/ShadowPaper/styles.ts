import styled from "styled-components";
import { colors } from "../../styles/style-config";

export const ShadowPaper = styled.div`
  padding: 12px;
  background-color: ${colors.backgroundColorLight};
  box-shadow: 1px 1px 5px 0 rgba(0, 0, 29, 0.22);
  display: flex;
`;
