import styled from 'styled-components'

export const Backdrop = styled.div`
  .loader {
    background: rgba(0, 0, 0, 0.5);
    height: 100vh;
    width: 100vw;
    position: fixed;
    top: 0;
    left: 0;
    z-index: 99;
  }
`
