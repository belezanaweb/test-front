import styled from 'styled-components'
import { colors } from '../../assets/styles/theme'

export const StyledCardInfoText = styled.p`
  font-size: 14px;
  font-weight: 400;
  line-height: 17px;
  text-transform: uppercase;
  color: ${colors['card-info-text']};
`
