import styled from 'styled-jss'

const Title = styled('h2')({
  color: '#999',
  fontWeight: 'bold',
  fontSize: 14,
  lineHeight: '20px',
  textTransform: 'uppercase',
  display: 'block',
  padding: [0, 10],
  margin: {
    top: 0,
    bottom: 5,
    left: 0,
    right: 0,
  },
})

export default Title
