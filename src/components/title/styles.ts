import styled from 'styled-components';

export const Title = styled.h1`
    color: #999;
    font-size: ${({theme}) => theme.fontSizes.h1};
    font-weight: 700;
    text-align: left;
    line-height: 17px;
    margin: 0 0 9px 11px;
`;