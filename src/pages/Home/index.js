import React, {useEffect} from 'react';
import ListProduct from '../ListProduct'

const Home = ({history}) =>{

  return (
    <div className="App">
      <ListProduct />
    </div>
  )

}

export default Home;
