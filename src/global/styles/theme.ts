import { DefaultTheme } from 'styled-components'

const defaultTheme: DefaultTheme = {
  borderRadius: '3px',

  colors: {
    primary: '#212122',
    secondary: '#999',
    accent: '#FF6C00',
    disabled: '#CCC',
    background: '#EEE',
    paper: '#FFF'
  }
}

export { defaultTheme }
