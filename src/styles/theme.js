const colors = {
  primary: '#ff6c00',
  primaryHover: '#d45a00',
  white: '#fff',
  gallery: '#eee',
  silver: '#ccc'
};

const widths = {
  container: '998px'
};

const fonts = {
  primary: {
    family: 'Helvetica Neue'
  },
  weights: {
    bold: 700
  }
};

export default { colors, fonts, widths };
