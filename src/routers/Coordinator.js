export const goToCart = (history) => {
  history.push('/cart')
}

export const goToPayment = (history) => {
  history.push('/payment')
}

export const goToConfirmation = (history) => {
  history.push('/confirmation')
}
