export interface ITheme {
  font: string;
  colors: {
    base: string,
    baseAux: string,
    baseDark: string,
    baseLight: string,
    error: string,
    focus: string,
    primary: string,
    primaryLight: string,
    primaryDark: string,
    secondary: string,
    secondaryLight: string,
    secondaryDark: string
  };
}
