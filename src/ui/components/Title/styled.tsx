import styled from 'styled-components/macro'

export const Title = styled.h1`
  color: ${({ theme }) => theme.colors.baseAux};
  font-size: 1.4rem;
  line-height: 1.7rem;
  margin: 1rem 0;
  text-transform: uppercase;
`
