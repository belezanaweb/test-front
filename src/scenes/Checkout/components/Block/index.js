import React, { Component } from 'react';

import { Container, ListItem, Figure } from './styles';

export default class Block extends Component {
  render() {
    return(
        <Container>
          
        </Container>
    );
  }
}
