
//---------------------------------
//  Session Reducer
//
//  We can set the variables to store here.

// Actions to comunicate with this reducer
/*import {
    
} from '../actions/types';*/

const INITIAL_STATE = {
    appName: 'Beleza na Web'
}

export default (state = INITIAL_STATE, action) => {

    switch (action.type) {
        default:
            return state;
    }

}
