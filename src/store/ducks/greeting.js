const initialState = {
  message: 'Simple boilerplate created using create-react-app!'
}

export default function greeting(state = initialState, action) {
  switch(action.type) {
    default:
      return state
  }
}
