import { render } from '../../utils/test'
import { InputMask } from './InputMask'

const renderComponent = () => {
  return render(<InputMask />)
}

test('Should be able to render component', () => {
  renderComponent()
})


